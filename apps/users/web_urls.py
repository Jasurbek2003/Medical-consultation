from django.urls import path
from . import views

app_name = 'users'

urlpatterns = [
    # Foydalanuvchi autentifikatsiya sahifalari (kelajakda)
    # path('login/', views.py.LoginView.as_view(), name='login'),
    # path('register/', views.py.RegisterView.as_view(), name='register'),
    # path('profile/', views.py.ProfileView.as_view(), name='profile'),

    # Hozircha bo'sh - kelajakda qo'shiladi
    # path('', views.py.UserListView.as_view(), name='list'),
]